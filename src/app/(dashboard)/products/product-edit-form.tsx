"use client";
import { useActionState } from "react";
import type { Product } from "@/lib/data";
import { PRODUCT_CATEGORIES } from "@/lib/product-categories";
import { deleteProduct, updateProduct, type ProductActionState } from "./actions";
import { ProductFields } from "./product-fields";
const initialState: ProductActionState = {};
export function ProductEditForm({ product, demo }: { product: Product; demo: boolean }) {
  const [state, action, pending] = useActionState(updateProduct, initialState); const [delState, delAction, delPending] = useActionState(deleteProduct, initialState); const disabled = demo || product.cost_price === undefined;
  const categories = PRODUCT_CATEGORIES.includes(product.category as (typeof PRODUCT_CATEGORIES)[number]) ? PRODUCT_CATEGORIES : [product.category, ...PRODUCT_CATEGORIES.filter((c) => c !== product.category)];
  return <div className="space-y-4"><form action={action} className="grid gap-4 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm sm:grid-cols-2"><input type="hidden" name="productId" value={product.id} /><ProductFields disabled={disabled} initial={{ brand: product.brand, nameZh: product.name_zh, nameEn: product.name_en, size: product.size, packageType: product.package_type, flavour: product.flavour }} /><label className="text-sm font-medium text-slate-700">Product code<input name="productCode" required defaultValue={product.product_code} disabled={disabled} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3" /></label><label className="text-sm font-medium text-slate-700">Category<select name="category" defaultValue={product.category} disabled={disabled} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3">{categories.map((c) => <option key={c} value={c}>{c}</option>)}</select></label><label className="text-sm font-medium text-slate-700">Barcode<input name="barcode" defaultValue={product.barcode ?? ""} disabled={disabled} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3" /></label><input type="hidden" name="name" value={product.name} /><label className="text-sm font-medium text-slate-700">Cost price<input name="costPrice" type="number" step="0.01" defaultValue={product.cost_price ?? 0} disabled={disabled} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3" /></label><label className="text-sm font-medium text-slate-700">Selling price<input name="sellingPrice" type="number" step="0.01" defaultValue={product.selling_price} disabled={disabled} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3" /></label><label className="text-sm font-medium text-slate-700">Minimum stock<input name="minimumStock" type="number" defaultValue={product.minimum_stock} disabled={disabled} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3" /></label><label className="text-sm font-medium text-slate-700">Status<select name="status" defaultValue={product.status} disabled={disabled} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3"><option value="active">Active</option><option value="inactive">Inactive</option></select></label><p className="text-sm text-slate-500 sm:col-span-2">Current stock: <strong>{product.current_stock}</strong>（請透過庫存流水帳調整）</p>{state.error ? <p role="alert" className="text-rose-700 sm:col-span-2">{state.error}</p> : null}<button type="submit" disabled={pending || disabled} className="h-11 rounded-xl bg-blue-700 px-5 text-sm font-semibold text-white disabled:bg-slate-300 sm:col-span-2">{pending ? "Saving…" : "Save changes"}</button></form><form action={delAction}><input type="hidden" name="productId" value={product.id} /><button disabled={delPending || disabled} className="h-11 rounded-xl border border-rose-300 px-5 text-sm text-rose-700">{delPending ? "Deleting…" : "Delete permanently"}</button>{delState.error ? <p className="text-rose-700">{delState.error}</p> : null}</form></div>;
}
